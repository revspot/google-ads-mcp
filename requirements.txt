google-ads>=25.0.0
# <2 is load-bearing: mcp 2.x renamed FastMCP to MCPServer and dropped
# mcp.server.fastmcp, which every module here imports. A bare >=1.1.0 resolves to 2.x
# today and the image fails to import at build time.
mcp>=1.1.0,<2
httpx>=0.27.0
pydantic>=2.0.0
google-auth-oauthlib>=1.0.0
PyYAML>=6.0.0
python-dotenv>=1.0.0
cachetools>=5.3.0
redis>=5.0.0

# Export and reporting dependencies
openpyxl>=3.1.2
reportlab>=4.0.0
matplotlib>=3.8.0
scipy>=1.11.0
statsmodels>=0.14.0

# HTTP transport (http_server.py); not needed for stdio use.
uvicorn>=0.30.0
